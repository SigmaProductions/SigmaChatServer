on:
  push:
    branches:
      - feature/cicd
  pull_request:
    branches:
      - feature/cicd
jobs:
  build:
    runs-on: [self-hosted, build]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Run a one-line script
      run: ls

      
